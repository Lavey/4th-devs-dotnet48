# Knowledge Base

Place your knowledge documents here (.md or .txt).
The agent will index them automatically on startup.

## Example

Add markdown or plain text files to this directory and they will be
indexed using hybrid search (BM25 + semantic vector similarity).
